using UnityEngine;

public enum InputType {
    None,
    Computer,
    Touch
}

public enum Modes {
    Normal,
    Build,
    Spectate
}

public enum UIState {
    Gameplay,
    SemiPause,
    Pause,
}

public enum AudioType {
    Main,
    Music,
    SFX,
    Environment
}

public enum AntiAliasing {
    Disabled,
    MSAA,
    FXAA
}

public enum OptionsType {
    VSync,
    AA,

}